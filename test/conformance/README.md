# Conformance Tests

Run the conformance suite:

```bash
pnpm conformance
```

To generate and serve the HTML report:

```bash
pnpm conformance:serve
```

Results are written to `test/conformance/results`, and the report is served at `http://localhost:8080`.
